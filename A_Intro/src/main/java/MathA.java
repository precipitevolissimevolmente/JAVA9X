/**
 * Created by CG on 4/5/2016.
 */
public class MathA implements MathUtil {
    @Override
    public int max(int a, int b) {
        return Math.max(a, b);
    }

    @Override
    public int abs(int element) {
        return Math.abs(element);
    }
}
